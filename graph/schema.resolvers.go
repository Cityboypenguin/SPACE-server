package graph

// This file will be automatically regenerated based on the schema, any resolver
// implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.88

import (
	"context"
	"errors"
	"fmt"
	"strconv"

	"github.com/Cityboypenguin/SPACE-server/graph/model"
	domainmodel "github.com/Cityboypenguin/SPACE-server/model"
)

// SignUp is the resolver for the signUp field.
func (r *mutationResolver) SignUp(ctx context.Context, in model.SignUpInput) (*model.User, error) {
	u, err := r.SignUpUseCase.Execute(ctx, in.Name)
	if err != nil {
		return nil, err
	}
	return toGraphQLUser(u), nil
}

// CreatePost is the resolver for the createPost field.
func (r *mutationResolver) CreatePost(ctx context.Context, in model.CreatePostInput) (*model.Post, error) {
	authorID, err := strconv.ParseInt(in.AuthorID, 10, 64)
	if err != nil {
		return nil, fmt.Errorf("invalid author id: %w", err)
	}

	result, err := r.CreatePostUseCase.Execute(ctx, domainmodel.CreatePostParam{
		AuthorID: authorID,
		Content:  in.Content,
	})
	if err != nil {
		return nil, fmt.Errorf("failed to create post: %w", err)
	}

	return toGraphQLPost(result.Post, result.Author), nil
}

// Hello is the resolver for the hello field.
func (r *queryResolver) Hello(ctx context.Context) (string, error) {
	return "hello", nil
}

// Users is the resolver for the users field.
func (r *queryResolver) Users(ctx context.Context) ([]*model.User, error) {
	users, err := r.GetUsersUseCase.Execute(ctx)
	if err != nil {
		return nil, fmt.Errorf("failed to get users: %w", err)
	}

	result := make([]*model.User, 0, len(users))
	for _, u := range users {
		result = append(result, toGraphQLUser(u))
	}
	return result, nil
}

// User is the resolver for the user field.
func (r *queryResolver) User(ctx context.Context, id string) (*model.User, error) {
	userID, err := strconv.ParseInt(id, 10, 64)
	if err != nil {
		return nil, fmt.Errorf("invalid user id: %w", err)
	}

	u, err := r.GetUserUseCase.Execute(ctx, userID)
	if err != nil {
		return nil, fmt.Errorf("failed to get user: %w", err)
	}
	if u == nil {
		return nil, errors.New("user not found")
	}
	return toGraphQLUser(u), nil
}

// Posts is the resolver for the posts field.
func (r *queryResolver) Posts(ctx context.Context) ([]*model.Post, error) {
	posts, err := r.GetPostsUseCase.Execute(ctx)
	if err != nil {
		return nil, fmt.Errorf("failed to get posts: %w", err)
	}

	// 全投稿のauthorIDを収集（重複排除）
	idSet := make(map[int64]struct{}, len(posts))
	for _, p := range posts {
		idSet[p.AuthorID] = struct{}{}
	}
	ids := make([]int64, 0, len(idSet))
	for id := range idSet {
		ids = append(ids, id)
	}

	// authorをまとめて1回で取得（N+1解消）
	users, err := r.GetUsersByIDsUseCase.Execute(ctx, ids)
	if err != nil {
		return nil, fmt.Errorf("failed to get authors: %w", err)
	}
	userMap := make(map[int64]*domainmodel.User, len(users))
	for _, u := range users {
		userMap[u.ID] = u
	}

	result := make([]*model.Post, 0, len(posts))
	for _, p := range posts {
		author, ok := userMap[p.AuthorID]
		if !ok {
			return nil, fmt.Errorf("author not found for post %d", p.ID)
		}
		result = append(result, toGraphQLPost(p, author))
	}

	return result, nil
}

// Mutation returns MutationResolver implementation.
func (r *Resolver) Mutation() MutationResolver { return &mutationResolver{r} }

// Query returns QueryResolver implementation.
func (r *Resolver) Query() QueryResolver { return &queryResolver{r} }

type mutationResolver struct{ *Resolver }
type queryResolver struct{ *Resolver }
